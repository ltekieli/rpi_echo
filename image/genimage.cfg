image sdcard.img {
  hdimage {
  }
 
  partition boot {
    partition-type = 0xC
    bootable = "true"
    image = "boot.vfat"
  }
 
  partition rootfs1 {
    partition-type = 0x83
    image = "rootfs.ext4"
  }

  partition rootfs2 {
    partition-type = 0x83
    image = "rootfs.ext4"
  }
}

